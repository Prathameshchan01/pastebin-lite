# Copy to .env and fill values as needed.
#
# NOTE: Do NOT commit your real .env to git.

# Optional: for deterministic TTL testing (grader uses this)
# TEST_MODE=1

# If you later switch to an external persistence layer (recommended for serverless),
# you can place credentials here.
# UPSTASH_REDIS_REST_URL = PLEASE_FILL_YOUR_URL_HERE
# UPSTASH_REDIS_REST_TOKEN= UPDATE_YOURS
